import React from 'react';
import './style.css';

const GreenButton = ({ text }) => {
    return (
        <button className="sec-btn primary-btn">{text}</button>
    );
};

export default GreenButton;
